.wrapper
  = render "header"

.main-card-done
  %h2 登録クレジットカード情報
  %br
  .show-card-number
    = "**** **** **** " + @default_card_information.last4
    %br
    - exp_month = @default_card_information.exp_month.to_s
    - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
    = exp_month + " / " + exp_year
    = form_with url:delete_card_index_path, id: 'charge-form', name: "inputForm", class: "show-card-btn" do
      %input{ type: "hidden", name: "card_id", value: "" }
      .show-card-delete-button 削除する
  .main-done__btn
    = link_to "/users/#{current_user.id}", class:"signup" do
      マイページへ戻る

.wrapper
  = render "footer"